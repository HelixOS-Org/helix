[package]
name = "lumina-derive"
version = "0.1.0"
edition = "2021"
authors = ["Helix OS Graphics Team"]
description = "Derive macros for Lumina graphics API"
license = "MIT OR Apache-2.0"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full", "extra-traits"] }
