[package]
name = "lumina-math"
version = "0.1.0"
edition = "2021"
authors = ["Helix OS Graphics Team"]
description = "Math library for Lumina graphics API"
license = "MIT OR Apache-2.0"

[features]
default = []
std = []

[dependencies]
