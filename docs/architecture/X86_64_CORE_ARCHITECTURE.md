# Helix OS Framework - x86_64 Core Architecture

## Design Document v1.0

**Date**: 2026-01-29
**Author**: Helix Architecture Team
**Status**: Implementation Ready

---

## 🎯 Executive Summary

Ce document définit l'architecture complète du cœur x86_64 pour Helix OS Framework.
Cette implémentation vise à supporter des **millions de lignes** de code kernel tout en
restant modulaire, maintenable et performante.

### Objectifs Clés

| Objectif | Description | Métrique |
|----------|-------------|----------|
| **Scalabilité** | Support kernel massif | 100k+ lignes HAL x86_64 |
| **Modularité** | Composants indépendants | 30+ modules |
| **Performance** | Latence minimale | < 1µs context switch |
| **SMP-Ready** | Multi-cœur dès le design | 256+ CPUs |
| **Extensibilité** | Ajout de features facile | Plugin architecture |

---

## 📁 Structure des Dossiers

```
hal/src/arch/x86_64/
├── mod.rs                          # Point d'entrée principal
├── README.md                       # Documentation du module
│
├── core/                           # Fondamentaux CPU (5,000+ lignes)
│   ├── mod.rs
│   ├── cpuid.rs                    # CPUID enumeration & features
│   ├── msr.rs                      # MSR framework complet
│   ├── control_regs.rs             # CR0-CR4, XCR0
│   ├── features.rs                 # CPU feature detection
│   ├── cache.rs                    # Cache management
│   └── fpu.rs                      # FPU/SSE/AVX state
│
├── segmentation/                   # GDT/TSS/LDT (3,000+ lignes)
│   ├── mod.rs
│   ├── gdt.rs                      # GDT management
│   ├── tss.rs                      # TSS per-CPU
│   ├── selectors.rs                # Segment selectors
│   └── descriptors.rs              # Descriptor types
│
├── interrupts/                     # IDT/Exceptions/IRQ (8,000+ lignes)
│   ├── mod.rs
│   ├── idt.rs                      # IDT management
│   ├── exceptions.rs               # Exception handlers
│   ├── handlers.rs                 # Handler generation
│   ├── stubs.rs                    # ASM stubs
│   ├── ist.rs                      # Interrupt Stack Table
│   ├── irq_routing.rs              # IRQ routing table
│   └── nmi.rs                      # NMI handling
│
├── apic/                           # APIC/IOAPIC (6,000+ lignes)
│   ├── mod.rs
│   ├── local_apic.rs               # Local APIC (xAPIC)
│   ├── x2apic.rs                   # x2APIC mode
│   ├── ioapic.rs                   # I/O APIC
│   ├── msi.rs                      # MSI/MSI-X support
│   ├── ipi.rs                      # Inter-Processor Interrupts
│   └── vectors.rs                  # Vector allocation
│
├── paging/                         # MMU/Paging (10,000+ lignes)
│   ├── mod.rs
│   ├── level4.rs                   # 4-level paging
│   ├── level5.rs                   # 5-level (LA57)
│   ├── page_table.rs               # Page table abstraction
│   ├── entries.rs                  # Page table entries
│   ├── flags.rs                    # Page flags
│   ├── tlb.rs                      # TLB management
│   ├── huge_pages.rs               # 2MB/1GB pages
│   ├── mapper.rs                   # Page mapper
│   ├── walker.rs                   # Page table walker
│   ├── frame_allocator.rs          # Frame allocation interface
│   └── kaslr.rs                    # KASLR integration
│
├── timers/                         # Timing subsystem (4,000+ lignes)
│   ├── mod.rs
│   ├── tsc.rs                      # TSC (invariant)
│   ├── hpet.rs                     # High Precision Event Timer
│   ├── apic_timer.rs               # Local APIC timer
│   ├── pit.rs                      # Legacy PIT (calibration)
│   ├── calibration.rs              # Timer calibration
│   └── time_api.rs                 # Unified time API
│
├── smp/                            # Multi-processor (5,000+ lignes)
│   ├── mod.rs
│   ├── ap_boot.rs                  # AP startup sequence
│   ├── trampoline.rs               # Real-mode trampoline
│   ├── per_cpu.rs                  # Per-CPU data
│   ├── barriers.rs                 # Memory barriers
│   ├── spinlock.rs                 # Spinlocks
│   └── topology.rs                 # CPU topology
│
├── syscall/                        # System calls (2,000+ lignes)
│   ├── mod.rs
│   ├── entry.rs                    # SYSCALL/SYSRET
│   ├── compat.rs                   # 32-bit compatibility
│   └── table.rs                    # Syscall table
│
├── context/                        # Context switching (3,000+ lignes)
│   ├── mod.rs
│   ├── switch.rs                   # Context switch
│   ├── state.rs                    # CPU state
│   ├── fpu_context.rs              # FPU/SIMD state
│   └── extended.rs                 # Extended state (XSAVE)
│
├── debug/                          # Debug facilities (2,000+ lignes)
│   ├── mod.rs
│   ├── breakpoints.rs              # Hardware breakpoints (DR0-DR7)
│   ├── trace.rs                    # Tracing support
│   └── dump.rs                     # State dumping
│
├── asm/                            # Assembly code (2,000+ lignes)
│   ├── boot.S                      # Boot code
│   ├── entry.S                     # Entry points
│   ├── switch.S                    # Context switch
│   ├── trampoline.S                # SMP trampoline
│   └── handlers.S                  # Interrupt stubs
│
└── tests/                          # Tests unitaires
    ├── mod.rs
    ├── cpuid_tests.rs
    ├── msr_tests.rs
    ├── paging_tests.rs
    └── apic_tests.rs
```

**Estimation totale: 50,000+ lignes de Rust pur pour le HAL x86_64**

---

## 🏗️ Architecture Détaillée

### 1. Core CPU Framework

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           CPU CORE FRAMEWORK                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │   CPUID      │  │    MSRs      │  │  Control     │  │   Features   │ │
│  │  Enumeration │  │  Framework   │  │  Registers   │  │  Detection   │ │
│  ├──────────────┤  ├──────────────┤  ├──────────────┤  ├──────────────┤ │
│  │ • Vendor ID  │  │ • EFER       │  │ • CR0        │  │ • SSE        │ │
│  │ • Family     │  │ • STAR       │  │ • CR2        │  │ • AVX        │ │
│  │ • Model      │  │ • LSTAR      │  │ • CR3        │  │ • AVX-512    │ │
│  │ • Stepping   │  │ • CSTAR      │  │ • CR4        │  │ • RDRAND     │ │
│  │ • Cache      │  │ • SFMASK     │  │ • XCR0       │  │ • RDSEED     │ │
│  │ • Features   │  │ • FS_BASE    │  │ • CR8        │  │ • SMAP       │ │
│  │ • Topology   │  │ • GS_BASE    │  │              │  │ • SMEP       │ │
│  │              │  │ • KERNEL_GS  │  │              │  │ • UMIP       │ │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘ │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

#### MSR Framework

```rust
/// MSR namespace with type-safe accessors
pub mod msr {
    // Extended Feature Enable Register
    pub const IA32_EFER: u32 = 0xC000_0080;
    pub const IA32_STAR: u32 = 0xC000_0081;
    pub const IA32_LSTAR: u32 = 0xC000_0082;
    pub const IA32_CSTAR: u32 = 0xC000_0083;
    pub const IA32_SFMASK: u32 = 0xC000_0084;
    pub const IA32_FS_BASE: u32 = 0xC000_0100;
    pub const IA32_GS_BASE: u32 = 0xC000_0101;
    pub const IA32_KERNEL_GS_BASE: u32 = 0xC000_0102;
    pub const IA32_TSC_AUX: u32 = 0xC000_0103;

    // APIC
    pub const IA32_APIC_BASE: u32 = 0x1B;
    pub const IA32_X2APIC_APICID: u32 = 0x802;

    // Time
    pub const IA32_TSC: u32 = 0x10;
    pub const IA32_TSC_ADJUST: u32 = 0x3B;

    // MTRR
    pub const IA32_MTRR_CAP: u32 = 0xFE;
    pub const IA32_MTRR_DEF_TYPE: u32 = 0x2FF;

    // PAT
    pub const IA32_PAT: u32 = 0x277;

    // Power
    pub const IA32_MISC_ENABLE: u32 = 0x1A0;
    pub const IA32_PERF_CTL: u32 = 0x199;
}
```

### 2. Segmentation Framework

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        SEGMENTATION FRAMEWORK                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  GDT Layout (Per-CPU for SMP):                                          │
│  ┌────────┬─────────────────────────────────────────────────────────┐   │
│  │ Index  │ Segment                                                  │   │
│  ├────────┼─────────────────────────────────────────────────────────┤   │
│  │ 0x00   │ Null Descriptor (required)                              │   │
│  │ 0x08   │ Kernel Code 64-bit (CS)                                 │   │
│  │ 0x10   │ Kernel Data (DS, SS, ES, FS, GS)                        │   │
│  │ 0x18   │ User Data 64-bit (for SYSRET order)                     │   │
│  │ 0x20   │ User Code 64-bit                                        │   │
│  │ 0x28   │ User Code 32-bit (compat mode)                          │   │
│  │ 0x30   │ TSS Low (16 bytes total)                                │   │
│  │ 0x38   │ TSS High                                                │   │
│  │ 0x40+  │ Per-CPU extended segments                               │   │
│  └────────┴─────────────────────────────────────────────────────────┘   │
│                                                                          │
│  TSS Per-CPU (104 bytes each):                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐│
│  │ RSP0: Kernel stack for ring 0                                       ││
│  │ RSP1: (unused in modern OS)                                         ││
│  │ RSP2: (unused in modern OS)                                         ││
│  │ IST1: Double Fault stack (16KB)                                     ││
│  │ IST2: Page Fault stack (16KB)                                       ││
│  │ IST3: NMI stack (8KB)                                               ││
│  │ IST4: Machine Check stack (8KB)                                     ││
│  │ IST5: Debug stack (8KB)                                             ││
│  │ IST6: Breakpoint stack (8KB)                                        ││
│  │ IST7: General exception stack (8KB)                                 ││
│  │ IOPB: I/O Permission Bitmap offset                                  ││
│  └─────────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────┘
```

### 3. Interrupt Framework

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        INTERRUPT FRAMEWORK                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  Vector Allocation (256 vectors):                                        │
│  ┌────────────┬──────────────────────────────────────────────────────┐  │
│  │ 0x00-0x1F  │ CPU Exceptions (reserved by Intel)                    │  │
│  │            │ 0x00: #DE Divide Error                                │  │
│  │            │ 0x01: #DB Debug                                       │  │
│  │            │ 0x02: NMI                                             │  │
│  │            │ 0x03: #BP Breakpoint                                  │  │
│  │            │ 0x04: #OF Overflow                                    │  │
│  │            │ 0x05: #BR Bound Range                                 │  │
│  │            │ 0x06: #UD Invalid Opcode                              │  │
│  │            │ 0x07: #NM Device Not Available                        │  │
│  │            │ 0x08: #DF Double Fault (IST1)                         │  │
│  │            │ 0x09: (legacy) Coprocessor Segment                    │  │
│  │            │ 0x0A: #TS Invalid TSS                                 │  │
│  │            │ 0x0B: #NP Segment Not Present                         │  │
│  │            │ 0x0C: #SS Stack Fault                                 │  │
│  │            │ 0x0D: #GP General Protection                          │  │
│  │            │ 0x0E: #PF Page Fault (IST2)                           │  │
│  │            │ 0x10: #MF x87 FP Exception                            │  │
│  │            │ 0x11: #AC Alignment Check                             │  │
│  │            │ 0x12: #MC Machine Check (IST3)                        │  │
│  │            │ 0x13: #XM SIMD Exception                              │  │
│  │            │ 0x14: #VE Virtualization                              │  │
│  │            │ 0x15: #CP Control Protection                          │  │
│  ├────────────┼──────────────────────────────────────────────────────┤  │
│  │ 0x20-0x2F  │ Legacy PIC IRQs (remapped)                            │  │
│  │            │ 0x20: Timer (IRQ0)                                    │  │
│  │            │ 0x21: Keyboard (IRQ1)                                 │  │
│  │            │ 0x22: Cascade (IRQ2)                                  │  │
│  │            │ 0x23-0x2F: Other IRQs                                 │  │
│  ├────────────┼──────────────────────────────────────────────────────┤  │
│  │ 0x30-0x7F  │ IOAPIC IRQs (80 vectors)                              │  │
│  ├────────────┼──────────────────────────────────────────────────────┤  │
│  │ 0x80       │ Syscall (legacy int 0x80)                             │  │
│  ├────────────┼──────────────────────────────────────────────────────┤  │
│  │ 0x81-0xEF  │ MSI/MSI-X vectors (111 vectors)                       │  │
│  ├────────────┼──────────────────────────────────────────────────────┤  │
│  │ 0xF0-0xFD  │ IPI vectors                                           │  │
│  │            │ 0xF0: Reschedule IPI                                  │  │
│  │            │ 0xF1: TLB Shootdown IPI                               │  │
│  │            │ 0xF2: Call Function IPI                               │  │
│  │            │ 0xF3: Call Function Single IPI                        │  │
│  ├────────────┼──────────────────────────────────────────────────────┤  │
│  │ 0xFE       │ APIC Error                                            │  │
│  │ 0xFF       │ Spurious Interrupt                                    │  │
│  └────────────┴──────────────────────────────────────────────────────┘  │
│                                                                          │
│  Handler Generation (Macro-based):                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐│
│  │ exception_handler!(divide_error, 0x00, false, |frame| {...})        ││
│  │ exception_handler!(page_fault, 0x0E, true, |frame, code| {...})     ││
│  │ irq_handler!(timer, 0x20, |frame| {...})                            ││
│  └─────────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────┘
```

### 4. Paging Framework

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           PAGING FRAMEWORK                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  4-Level Paging (Standard):                                             │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ Virtual Address (48-bit):                                          │ │
│  │ ┌──────┬───────┬───────┬───────┬───────┬────────────────────────┐  │ │
│  │ │ Sign │ PML4  │ PDPT  │  PD   │  PT   │       Offset           │  │ │
│  │ │ Ext  │ [9]   │ [9]   │ [9]   │ [9]   │        [12]            │  │ │
│  │ │63-48 │47-39  │38-30  │29-21  │20-12  │        11-0            │  │ │
│  │ └──────┴───────┴───────┴───────┴───────┴────────────────────────┘  │ │
│  │                                                                     │ │
│  │ Page Sizes: 4KB (PT), 2MB (PD), 1GB (PDPT)                         │ │
│  │ Virtual Space: 256 TiB (48-bit canonical)                          │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                          │
│  5-Level Paging (LA57):                                                 │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ Virtual Address (57-bit):                                          │ │
│  │ ┌──────┬───────┬───────┬───────┬───────┬───────┬────────────────┐  │ │
│  │ │ Sign │ PML5  │ PML4  │ PDPT  │  PD   │  PT   │    Offset      │  │ │
│  │ │ Ext  │ [9]   │ [9]   │ [9]   │ [9]   │ [9]   │     [12]       │  │ │
│  │ │63-57 │56-48  │47-39  │38-30  │29-21  │20-12  │     11-0       │  │ │
│  │ └──────┴───────┴───────┴───────┴───────┴───────┴────────────────┘  │ │
│  │                                                                     │ │
│  │ Virtual Space: 128 PiB (57-bit canonical)                          │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                          │
│  Page Table Entry Flags:                                                │
│  ┌─────┬──────────────────────────────────────────────────────────────┐ │
│  │ Bit │ Flag                                                         │ │
│  ├─────┼──────────────────────────────────────────────────────────────┤ │
│  │  0  │ Present (P)                                                  │ │
│  │  1  │ Read/Write (R/W)                                             │ │
│  │  2  │ User/Supervisor (U/S)                                        │ │
│  │  3  │ Page-level Write-Through (PWT)                               │ │
│  │  4  │ Page-level Cache Disable (PCD)                               │ │
│  │  5  │ Accessed (A)                                                 │ │
│  │  6  │ Dirty (D) - leaf only                                        │ │
│  │  7  │ Page Size (PS) or PAT (leaf)                                 │ │
│  │  8  │ Global (G) - leaf only                                       │ │
│  │ 9-11│ Available for OS                                             │ │
│  │ 12+ │ Physical Address (aligned to page size)                      │ │
│  │ 52-58│ Available for OS (Protection Key in some modes)             │ │
│  │ 59-62│ Available for OS                                            │ │
│  │ 63  │ Execute Disable (XD/NX)                                      │ │
│  └─────┴──────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
```

### 5. APIC Framework

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            APIC FRAMEWORK                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  Local APIC (Per-CPU):                                                  │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ Base Address: IA32_APIC_BASE MSR (usually 0xFEE00000)              │ │
│  │                                                                     │ │
│  │ Registers:                                                          │ │
│  │ ┌──────────┬────────────────────────────────────────────────────┐  │ │
│  │ │ Offset   │ Register                                            │  │ │
│  │ ├──────────┼────────────────────────────────────────────────────┤  │ │
│  │ │ 0x020    │ Local APIC ID                                       │  │ │
│  │ │ 0x030    │ Local APIC Version                                  │  │ │
│  │ │ 0x080    │ Task Priority Register (TPR)                        │  │ │
│  │ │ 0x090    │ Arbitration Priority Register (APR)                 │  │ │
│  │ │ 0x0A0    │ Processor Priority Register (PPR)                   │  │ │
│  │ │ 0x0B0    │ EOI Register                                        │  │ │
│  │ │ 0x0D0    │ Logical Destination Register (LDR)                  │  │ │
│  │ │ 0x0E0    │ Destination Format Register (DFR)                   │  │ │
│  │ │ 0x0F0    │ Spurious Interrupt Vector Register                  │  │ │
│  │ │ 0x100-170│ In-Service Registers (ISR)                          │  │ │
│  │ │ 0x180-1F0│ Trigger Mode Registers (TMR)                        │  │ │
│  │ │ 0x200-270│ Interrupt Request Registers (IRR)                   │  │ │
│  │ │ 0x280    │ Error Status Register (ESR)                         │  │ │
│  │ │ 0x300    │ Interrupt Command Register Low (ICR)                │  │ │
│  │ │ 0x310    │ Interrupt Command Register High                     │  │ │
│  │ │ 0x320    │ LVT Timer                                           │  │ │
│  │ │ 0x330    │ LVT Thermal Sensor                                  │  │ │
│  │ │ 0x340    │ LVT Performance Counter                             │  │ │
│  │ │ 0x350    │ LVT LINT0                                           │  │ │
│  │ │ 0x360    │ LVT LINT1                                           │  │ │
│  │ │ 0x370    │ LVT Error                                           │  │ │
│  │ │ 0x380    │ Initial Count Register                              │  │ │
│  │ │ 0x390    │ Current Count Register                              │  │ │
│  │ │ 0x3E0    │ Divide Configuration Register                       │  │ │
│  │ └──────────┴────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                          │
│  I/O APIC:                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ Base Address: Usually 0xFEC00000 (from MADT)                       │ │
│  │                                                                     │ │
│  │ Registers (indirect access via IOREGSEL/IOWIN):                    │ │
│  │ ┌──────────┬────────────────────────────────────────────────────┐  │ │
│  │ │ Index    │ Register                                            │  │ │
│  │ ├──────────┼────────────────────────────────────────────────────┤  │ │
│  │ │ 0x00     │ IOAPIC ID                                           │  │ │
│  │ │ 0x01     │ IOAPIC Version (includes max redirection entries)   │  │ │
│  │ │ 0x02     │ IOAPIC Arbitration ID                               │  │ │
│  │ │ 0x10+2n  │ Redirection Entry n (64-bit, low)                   │  │ │
│  │ │ 0x11+2n  │ Redirection Entry n (64-bit, high)                  │  │ │
│  │ └──────────┴────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
```

### 6. Timer Framework

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            TIMER FRAMEWORK                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  Timer Sources (par ordre de priorité):                                 │
│  ┌─────────────────────────────────────────────────────────────────────┐│
│  │                                                                      ││
│  │  1. TSC (Time Stamp Counter)                                        ││
│  │     ├── Invariant TSC (préféré)                                     ││
│  │     ├── Nonstop TSC (C-states)                                      ││
│  │     ├── RDTSCP (processor ID)                                       ││
│  │     └── Calibration via CPUID.15H ou PIT/HPET                       ││
│  │                                                                      ││
│  │  2. HPET (High Precision Event Timer)                               ││
│  │     ├── 10 MHz+ (100ns resolution)                                  ││
│  │     ├── Up to 32 comparators                                        ││
│  │     └── ACPI table: HPET                                            ││
│  │                                                                      ││
│  │  3. APIC Timer                                                      ││
│  │     ├── One-shot mode                                               ││
│  │     ├── Periodic mode                                               ││
│  │     ├── TSC-deadline mode (modern CPUs)                             ││
│  │     └── Per-CPU (no contention)                                     ││
│  │                                                                      ││
│  │  4. PIT (Legacy - calibration only)                                 ││
│  │     └── 1.193182 MHz (fixed frequency)                              ││
│  │                                                                      ││
│  └─────────────────────────────────────────────────────────────────────┘│
│                                                                          │
│  Calibration Sequence:                                                  │
│  ┌─────────────────────────────────────────────────────────────────────┐│
│  │  1. Try CPUID.15H (native TSC frequency)                            ││
│  │  2. Try CPUID.16H (processor base frequency)                        ││
│  │  3. Try MSR_PLATFORM_INFO (on Intel)                                ││
│  │  4. Calibrate using HPET (if available)                             ││
│  │  5. Calibrate using PIT (fallback)                                  ││
│  │  6. Cross-validate TSC vs APIC timer                                ││
│  └─────────────────────────────────────────────────────────────────────┘│
│                                                                          │
│  Unified Time API:                                                      │
│  ┌─────────────────────────────────────────────────────────────────────┐│
│  │  time::now_ns() -> u64           // Nanoseconds since boot          ││
│  │  time::now_tsc() -> u64          // Raw TSC value                   ││
│  │  time::tsc_to_ns(tsc) -> u64     // Convert TSC to ns               ││
│  │  time::delay_ns(ns)              // Busy-wait delay                 ││
│  │  time::set_timer(ns, callback)   // One-shot timer                  ││
│  │  time::set_periodic(ns, callback)// Periodic timer                  ││
│  └─────────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────┘
```

### 7. SMP Framework

```
┌─────────────────────────────────────────────────────────────────────────┐
│                             SMP FRAMEWORK                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  AP Boot Sequence:                                                      │
│  ┌─────────────────────────────────────────────────────────────────────┐│
│  │                                                                      ││
│  │  BSP (Bootstrap Processor):                                         ││
│  │  1. Parse MADT for AP APIC IDs                                      ││
│  │  2. Allocate AP stacks (per-CPU)                                    ││
│  │  3. Copy trampoline to 0x8000 (real mode address)                   ││
│  │  4. For each AP:                                                    ││
│  │     a. Send INIT IPI                                                ││
│  │     b. Wait 10ms                                                    ││
│  │     c. Send SIPI (Startup IPI) to 0x8000                            ││
│  │     d. Wait for AP to signal ready                                  ││
│  │                                                                      ││
│  │  AP (Application Processor):                                        ││
│  │  1. Start in real mode at trampoline                                ││
│  │  2. Switch to protected mode                                        ││
│  │  3. Switch to long mode                                             ││
│  │  4. Load per-CPU GDT/IDT                                            ││
│  │  5. Initialize local APIC                                           ││
│  │  6. Signal ready to BSP                                             ││
│  │  7. Wait for work                                                   ││
│  │                                                                      ││
│  └─────────────────────────────────────────────────────────────────────┘│
│                                                                          │
│  Per-CPU Data:                                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐│
│  │  #[repr(C)]                                                         ││
│  │  pub struct PerCpuData {                                            ││
│  │      // Self-pointer for fast access                                ││
│  │      self_ptr: *const PerCpuData,                                   ││
│  │      // CPU identification                                          ││
│  │      cpu_id: u32,                                                   ││
│  │      apic_id: u32,                                                  ││
│  │      // Current execution context                                   ││
│  │      current_task: *mut Task,                                       ││
│  │      kernel_stack: u64,                                             ││
│  │      user_stack: u64,                                               ││
│  │      // TSS for this CPU                                            ││
│  │      tss: *mut TaskStateSegment,                                    ││
│  │      // Local APIC                                                  ││
│  │      apic_base: u64,                                                ││
│  │      // Scheduler state                                             ││
│  │      scheduler: *mut Scheduler,                                     ││
│  │      // Statistics                                                  ││
│  │      irq_count: u64,                                                ││
│  │      context_switches: u64,                                         ││
│  │      // Scratch space                                               ││
│  │      scratch: [u64; 16],                                            ││
│  │  }                                                                  ││
│  │                                                                      ││
│  │  // Access via GS segment (KERNEL_GS_BASE MSR)                      ││
│  │  pub fn current_cpu() -> &'static PerCpuData {                      ││
│  │      unsafe { &*read_gs_offset::<*const PerCpuData>(0) }            ││
│  │  }                                                                  ││
│  └─────────────────────────────────────────────────────────────────────┘│
│                                                                          │
│  IPI Types:                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐│
│  │  • RESCHEDULE: Notify CPU to reschedule                             ││
│  │  • TLB_SHOOTDOWN: Invalidate TLB entries                            ││
│  │  • CALL_FUNCTION: Execute function on remote CPU                    ││
│  │  • CALL_FUNCTION_SINGLE: Execute on specific CPU                    ││
│  │  • STOP: Stop CPU (for reboot/shutdown)                             ││
│  │  • NMI: Non-maskable interrupt (debugging)                          ││
│  └─────────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔧 Interfaces Clés

### CPU Abstraction

```rust
/// CPU capabilities detected at runtime
pub struct CpuCapabilities {
    // Basic features
    pub sse: bool,
    pub sse2: bool,
    pub sse3: bool,
    pub ssse3: bool,
    pub sse4_1: bool,
    pub sse4_2: bool,
    pub avx: bool,
    pub avx2: bool,
    pub avx512f: bool,

    // Security features
    pub smep: bool,           // Supervisor Mode Execution Prevention
    pub smap: bool,           // Supervisor Mode Access Prevention
    pub umip: bool,           // User-Mode Instruction Prevention
    pub pku: bool,            // Protection Keys for Userspace

    // Virtualization
    pub vmx: bool,            // Intel VT-x
    pub svm: bool,            // AMD-V

    // Memory
    pub la57: bool,           // 5-level paging
    pub nx: bool,             // No-Execute
    pub pcid: bool,           // Process-Context Identifiers
    pub invpcid: bool,        // INVPCID instruction

    // APIC
    pub x2apic: bool,
    pub tsc_deadline: bool,

    // Time
    pub invariant_tsc: bool,
    pub tsc_adjust: bool,
    pub rdtscp: bool,

    // Random
    pub rdrand: bool,
    pub rdseed: bool,

    // Power
    pub monitor_mwait: bool,
}
```

### Page Table Abstraction

```rust
/// Page table interface (polymorphic over 4/5 level)
pub trait PageTable: Send + Sync {
    /// Map a virtual address to a physical address
    fn map(
        &mut self,
        virt: VirtAddr,
        phys: PhysAddr,
        size: PageSize,
        flags: PageFlags,
    ) -> Result<(), PagingError>;

    /// Unmap a virtual address
    fn unmap(&mut self, virt: VirtAddr, size: PageSize) -> Result<PhysAddr, PagingError>;

    /// Translate virtual to physical
    fn translate(&self, virt: VirtAddr) -> Option<PhysAddr>;

    /// Update flags for a mapping
    fn update_flags(&mut self, virt: VirtAddr, flags: PageFlags) -> Result<(), PagingError>;

    /// Flush TLB for an address
    fn flush(&self, virt: VirtAddr);

    /// Flush entire TLB
    fn flush_all(&self);

    /// Get the physical address of the root table
    fn root_phys(&self) -> PhysAddr;
}

/// Page sizes supported
pub enum PageSize {
    Kib4,    // 4 KiB
    Mib2,    // 2 MiB
    Gib1,    // 1 GiB
}

/// Page flags
bitflags! {
    pub struct PageFlags: u64 {
        const PRESENT = 1 << 0;
        const WRITABLE = 1 << 1;
        const USER = 1 << 2;
        const WRITE_THROUGH = 1 << 3;
        const NO_CACHE = 1 << 4;
        const ACCESSED = 1 << 5;
        const DIRTY = 1 << 6;
        const HUGE = 1 << 7;
        const GLOBAL = 1 << 8;
        const NO_EXECUTE = 1 << 63;

        // Common combinations
        const KERNEL_RO = Self::PRESENT.bits();
        const KERNEL_RW = Self::PRESENT.bits() | Self::WRITABLE.bits();
        const KERNEL_CODE = Self::PRESENT.bits();
        const KERNEL_DATA = Self::PRESENT.bits() | Self::WRITABLE.bits() | Self::NO_EXECUTE.bits();
        const USER_RO = Self::PRESENT.bits() | Self::USER.bits();
        const USER_RW = Self::PRESENT.bits() | Self::USER.bits() | Self::WRITABLE.bits();
        const USER_CODE = Self::PRESENT.bits() | Self::USER.bits();
        const USER_DATA = Self::PRESENT.bits() | Self::USER.bits() | Self::WRITABLE.bits() | Self::NO_EXECUTE.bits();
    }
}
```

### APIC Abstraction

```rust
/// Local APIC interface
pub trait LocalApic: Send + Sync {
    /// Get APIC ID
    fn id(&self) -> u32;

    /// Get APIC version
    fn version(&self) -> u32;

    /// Enable the local APIC
    fn enable(&mut self);

    /// Send End Of Interrupt
    fn eoi(&self);

    /// Set spurious interrupt vector
    fn set_spurious_vector(&mut self, vector: u8);

    /// Configure timer
    fn set_timer(&mut self, config: TimerConfig);

    /// Start timer in one-shot mode
    fn timer_oneshot(&mut self, ticks: u32);

    /// Start timer in periodic mode
    fn timer_periodic(&mut self, ticks: u32);

    /// Stop timer
    fn timer_stop(&mut self);

    /// Send IPI to another CPU
    fn send_ipi(&self, dest: IpiDestination, vector: u8);

    /// Send INIT IPI
    fn send_init(&self, dest: IpiDestination);

    /// Send SIPI (Startup IPI)
    fn send_sipi(&self, dest: IpiDestination, vector: u8);
}

/// IOAPIC interface
pub trait IoApic: Send + Sync {
    /// Get IOAPIC ID
    fn id(&self) -> u8;

    /// Get maximum redirection entry
    fn max_entry(&self) -> u8;

    /// Configure a redirection entry
    fn set_entry(&mut self, index: u8, entry: RedirectionEntry);

    /// Get a redirection entry
    fn get_entry(&self, index: u8) -> RedirectionEntry;

    /// Mask an IRQ
    fn mask(&mut self, irq: u8);

    /// Unmask an IRQ
    fn unmask(&mut self, irq: u8);
}
```

---

## 📊 Metrics & Objectifs

| Composant | Lignes Estimées | Complexité | Priorité |
|-----------|-----------------|------------|----------|
| Core CPU | 5,000 | Haute | P0 |
| Segmentation | 3,000 | Moyenne | P0 |
| Interrupts | 8,000 | Très haute | P0 |
| APIC | 6,000 | Haute | P1 |
| Paging | 10,000 | Très haute | P0 |
| Timers | 4,000 | Haute | P1 |
| SMP | 5,000 | Très haute | P1 |
| Syscall | 2,000 | Moyenne | P1 |
| Context | 3,000 | Haute | P0 |
| Debug | 2,000 | Moyenne | P2 |
| ASM | 2,000 | Haute | P0 |
| **Total** | **50,000+** | - | - |

---

## 🛣️ Roadmap d'Implémentation

### Phase 1: Core Foundation (Semaine 1-2)
- [ ] Réorganiser la structure des dossiers
- [ ] Implémenter le MSR framework complet
- [ ] CPUID enumeration avancée
- [ ] Control registers abstraction

### Phase 2: Segmentation & Interrupts (Semaine 3-4)
- [ ] GDT per-CPU avec multi-TSS
- [ ] IDT framework avec génération automatique
- [ ] IST complet
- [ ] Exception handlers

### Phase 3: Paging (Semaine 5-6)
- [ ] 4-level paging complet
- [ ] 5-level paging (LA57)
- [ ] Page mapper/walker
- [ ] TLB management
- [ ] Huge pages

### Phase 4: APIC & Timers (Semaine 7-8)
- [ ] Local APIC (xAPIC + x2APIC)
- [ ] IOAPIC
- [ ] TSC calibration
- [ ] HPET driver
- [ ] APIC timer

### Phase 5: SMP (Semaine 9-10)
- [ ] AP boot sequence
- [ ] Real-mode trampoline
- [ ] Per-CPU data
- [ ] IPI support

---

## 🔒 Considérations de Sécurité

| Feature | Description | Status |
|---------|-------------|--------|
| SMEP | Prevent kernel exec of user pages | Planned |
| SMAP | Prevent kernel access to user pages | Planned |
| UMIP | Hide privileged info from userspace | Planned |
| KPTI | Kernel Page Table Isolation | Planned |
| KASLR | Kernel Address Randomization | ✅ Done |
| NX | Non-executable memory | Planned |
| Stack Canaries | Stack overflow detection | Planned |

---

## 📚 Références

- Intel® 64 and IA-32 Architectures Software Developer's Manual
- AMD64 Architecture Programmer's Manual
- ACPI Specification 6.5
- UEFI Specification 2.10
- OSDev Wiki

---

*Document généré par l'équipe Architecture Helix OS*
