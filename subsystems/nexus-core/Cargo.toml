[package]
name = "helix-nexus-core"
version = "0.1.0"
edition = "2024"
authors = ["Helix OS Team"]
description = "Core infrastructure for the NEXUS cognitive kernel subsystem"
license = "MIT OR Apache-2.0"
repository = "https://github.com/helix-os/helix"
keywords = ["kernel", "os", "core", "cognitive", "ai"]
categories = ["no-std", "data-structures"]

[lib]
name = "helix_nexus_core"
path = "src/lib.rs"

[features]
default = []
std = []
alloc = []

[dependencies]
helix-nexus-types = { path = "../nexus-types" }
libm = "0.2"
