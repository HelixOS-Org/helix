[package]
name = "helix-nexus-types"
version = "0.1.0"
edition = "2024"
authors = ["Helix OS Team"]
description = "Core type definitions for the NEXUS cognitive kernel subsystem"
license = "MIT OR Apache-2.0"
repository = "https://github.com/helix-os/helix"
keywords = ["kernel", "os", "types", "cognitive", "ai"]
categories = ["no-std", "data-structures"]
readme.workspace = true

[lib]
name = "helix_nexus_types"
path = "src/lib.rs"

[features]
default = []
std = []
alloc = []

[dependencies]
# Minimal dependencies for type definitions
